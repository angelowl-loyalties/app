name: Continuous Integration (Authorizer)

on:
  push:
    branches: [ '**' ]
    paths:
      - "authorizer/**"
      - .github/workflows/ci_authorizer.yml
      - .github/workflows/workflow_lambda.yml
  pull_request:
    branches: [ master ]
    paths:
      - "authorizer/**"
      - .github/workflows/ci_authorizer.yml
      - .github/workflows/workflow_lambda.yml

permissions:
  id-token: write
  contents: read 

jobs:
  lint:
    name: Golang Linting
    uses: ./.github/workflows/golangci-lint.yml
    with:
      workdir: 'authorizer'
  sast:
    needs: lint
    name: Golang SAST
    uses: ./.github/workflows/sast_golang.yml
    with:
      workdir: 'authorizer'
  ci:
    needs: [lint, sast]
    name: Continuous Integration
    uses: ./.github/workflows/workflow_auth_lambda.yml
    with:
      dockerfile: 'Dockerfile'
      workdir: 'authorizer'